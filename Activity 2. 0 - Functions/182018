#include <stdio.h>
int main() {
    int a,b,c,d,e,average;
    int sum=0;

    printf("Enter five elements:\n");
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    
    sum = a+b+c+d+e;
    average = sum / 5;
    printf("Average = %d", average);
    return 0;
}